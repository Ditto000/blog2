extends ../layout

block body
	h2 Bulletin Board
	.list-group
		each post in posts
			a.list-group-item(href=`/blog/${post.slug}`)= post.title
	div
		a.btn.btn-primary.btn-lg(href='/blog/new') New Blog Post
